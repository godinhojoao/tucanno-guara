// import { render } from '@testing-library/react';
// import App from './../App';

describe.skip('dale', () => {
  test('dale2', () => {
    // const { container } = render(<App />);
    // const element = container.querySelector('a');

    // expect(element).toBeInTheDocument();
    // expect(element?.textContent).toBe('Ver todos os animais');
    // expect(element?.getAttribute('href')).toBe('#/animais');
  });
});